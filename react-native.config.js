module.exports = {
  dependencies: {
    'react-native-vision-camera': {
      platforms: {
        android: null, // disable Android platform, might be auto-linked
        ios: null, // disable iOS platform, might be auto-linked
      },
    },
  },
};
